# Project delivery preference

The owner requests that completed changes in this project be committed, pushed
to its GitHub origin, and deployed to the live CHINO site by default. Run the
appropriate checks first, include new public assets in the release script, and
verify the live result. Commit only files relevant to the requested work.
If authentication or deployment permissions block publishing, report the
blocker and do not describe an undeployed change as live.
